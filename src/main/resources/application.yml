server:
  port: 8761
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: false
  instance:
    hostname: lfind
spring:
  application:
    name: lfind
